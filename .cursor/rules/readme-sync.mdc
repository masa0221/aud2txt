# README Sync

When editing README.md, also update README.ja.md with the same changes (in Japanese).

Both files should have parallel structure and mutual links at the top.

Consider this rule if it affects your changes.
